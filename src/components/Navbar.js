import { Link } from 'react-router-dom'; // <== IMPORT

function Navbar() {
  return (
    <nav className="Navbar">
      <div>
        <h1>WikiCountries</h1>
      </div>
    </nav>
  );
}

export default Navbar;
